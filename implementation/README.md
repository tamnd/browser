# Implementation notes

Release-by-release notes on what got built, what got cut, and why.
Each file tracks one release with a checklist that is updated as work lands, so a half-done release reads as half-done.
Version numbers match git tags, and every release bumps the patch number.
